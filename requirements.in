# PDF to OFX Converter - Source Dependencies
# ANZ Plus MVP - Minimal dependencies for core functionality
#
# This is the SOURCE file for dependencies.
# Edit this file, then run: pip-compile --generate-hashes requirements.in
# DO NOT edit requirements.txt directly - it is auto-generated.

# OFX Generation (CRITICAL - using ofxtools instead of manual SGML)
ofxtools>=0.9.5

# PDF Processing
pdfplumber>=0.11.9

# Data Models & Validation
pydantic>=2.12.5
pandas>=1.4.0,<3.0

# Date Handling
python-dateutil>=2.9.0

# Utilities
protobuf>=4.0.0
werkzeug>=3.0.0

# Web Framework
fastapi>=0.128.0

# --- Server / Uvicorn Config ---
# Windows: Use basic uvicorn (no C-extensions)
uvicorn>=0.40.0; sys_platform == 'win32'

# Linux/Docker: Manually add speed boosters, skip 'watchfiles' (Rust)
uvicorn>=0.40.0; sys_platform != 'win32'
uvloop>=0.21.0; sys_platform != 'win32'
httptools>=0.6.4; sys_platform != 'win32'

# Dependencies
python-multipart>=0.0.22
jinja2>=3.1.6
aiofiles>=25.1.0

# Testing (for CI/CD)
pytest>=9.0.2
pytest-cov>=7.0.0

# Note: streamlit removed from production dependencies
# Optional: OCR (for scanned PDFs - not needed for ANZ Plus digital PDFs)
# pytesseract>=0.3.10
# opencv-python>=4.9.0.80
# Pillow>=10.2.0
